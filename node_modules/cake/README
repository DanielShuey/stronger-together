A fork of CoffeeScript's `cake` utility so that it might be used with e.g. `coffee-script-redux`.

  To install:
  npm install -g cake
  (Leave off the -g if you don't wish to install globally.)

  To suggest a feature, report a bug, or general discussion:
  http://github.com/shimaore/cake/issues/
